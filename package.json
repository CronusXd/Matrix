{
  "name": "matrix-engine",
  "version": "3.0.0",
  "private": true,
  "description": "Matrix — Model Amplification Engine. Maximizes LLM performance through externalized intelligence.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/seed.js",
    "benchmark:gap": "node src/benchmarks/model-gap-benchmark.js --format json",
    "lint": "eslint src/",
    "test": "echo 'Tests not yet migrated to new architecture' && exit 0"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0",
    "sql.js": "^1.12.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "nyc": "^18.0.0"
  }
}
